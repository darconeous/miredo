ToDo list:
===========
$Id$

This task list is only indicative. It is not fully comprehensive.
If you have an idea, please submit it to the miredo-devel mailing list.

(!) must be done before next release
(*) must be before stable release
( ) should probably be done someday if one finds time and interest
(?) should be evaluated for usefulness and/or feasibility first
(F) should be done, but probably not at the moment
(H) should be done, but a capable volunteer is required
(X) being taken care of (outside of miredo SVN)

Important features & fixes:
----------------------------
(*) do something in case of DoS against the peers list
( ) fixed TODOs and FIXMEs in source code
(*) local Teredo discovery
(*) interval determination procedure
(!) document libteredo-mire (manpage? --help, --version)

Not so important features:
---------------------------
( ) relay IPv6 access control lists:
    can be done with the OS firewall
(*) some statistics
(H) other localizations
( ) support for UPnP IGD to open an UDP port on supporting NATs
(?) “automatic sunset” / “exit strategy”

Conformance:
-------------
(*) limit sending of bubbles (not completed yet)
(*) send unreachable in response when dropping previously queued packets

Security:
----------
(?) full secure qualification support with user ID
( ) relay/server IPv4 _modifiable_ access control lists
    (currently hard-coded to reject private IPv4 space)
( ) automatic/dynamic relay access control list
    (stateful firewall-like)

Performance/scalability:
-------------------------
( ) check if libtun6 could use kernel checksumming
    or if it should disable it
( ) check if libteredo-server could use kernel for checksumming

Extension:
-----------
( ) support symmetric to non-symmetric NAT traversal
(?) support symmetric NAT traversal

Portability:
-------------
(H) big-endianess compatibility tests
(H) byte alignement tests on problematic platforms (not i386)
(H) Solaris: libtun6 support
(H) Solaris: tests & fixes
(H) Darwin: tests & fixes
(H) DragonFlyBSD: tests & fixes

Documentation & support:
-------------------------
( ) custom INSTALL file
( ) RPM packaging
(X) Debian packaging : see ITP http://bugs.debian.org/338128

Re-usability:
--------------
(F) libteredo API stabilization
( ) avoid syslog() in libtun6
( ) avoid syslog() in libteredo
